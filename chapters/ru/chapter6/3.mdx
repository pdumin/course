<FrameworkSwitchCourse {fw} />

# Специальные возможности быстрых токенизаторов [[fast-tokenizers-special-powers]]

{#if fw === 'pt'}

<CourseFloatingBanner chapter={6}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter6/section3_pt.ipynb"},
    {label: "Aws Studio", value: "https://studiolab.sagemaker.aws/import/github/huggingface/notebooks/blob/master/course/en/chapter6/section3_pt.ipynb"},
]} />

{:else}

<CourseFloatingBanner chapter={6}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter6/section3_tf.ipynb"},
    {label: "Aws Studio", value: "https://studiolab.sagemaker.aws/import/github/huggingface/notebooks/blob/master/course/en/chapter6/section3_tf.ipynb"},
]} />

{/if}

В этом разделе мы более подробно рассмотрим возможности токенизаторов в 🤗 Transformers. До сих пор мы использовали их только для токенизации входных данных или декодирования идентификаторов обратно в текст, но токенизаторы, особенно поддерживаемые библиотекой 🤗 Tokenizers, могут делать гораздо больше. Чтобы проиллюстрировать эти дополнительные функции, мы рассмотрим, как воспроизвести результаты пайплайнов `token-classification` (которую мы назвали `NER`) и `question-answering`, с которыми мы впервые столкнулись в [Главе 1](/course/ru/chapter1).

<Youtube id="g8quOxoqhHQ"/>

В дальнейшем обсуждении мы часто будем проводить различие между «медленными» и «быстрыми» токенизаторами. Медленные токенизаторы — это те, которые написаны на Python внутри библиотеки 🤗 Transformers, а быстрые версии — это те, которые предоставляются 🤗 Tokenizers, написанными на Rust. Если вы помните таблицу из [Главы 5](/course/ru/chapter5/3), в которой сообщалось, сколько времени потребовалось быстрому и медленному токенизаторам для токенизации набора данных обзора лекарств, вы должны иметь представление о том, почему мы называем их быстрыми и медленными:

                | Быстрый токенизатор | Медленный токенизатор
:--------------:|:--------------:|:-------------:
`batched=True`  | 10.8s          | 4min41s
`batched=False` | 59.2s          | 5min3s

<Tip warning={true}>

⚠️ При токенизации одного предложения вы не всегда увидите разницу в скорости между медленной и быстрой версиями одного и того же токенизатора. На самом деле, быстрая версия может быть медленнее! Только при параллельной токенизации большого количества текстов вы сможете четко увидеть разницу.

</Tip>

## Batch encoding[[batch-encoding]] (кодирование батчей)

<Youtube id="3umI3tm27Vw"/>

Вывод токенизатора — это не простой словарь Python; то, что мы получаем, на самом деле является специальным объектом `BatchEncoding`. Это подкласс словаря (вот почему раньше мы могли индексировать этот результат без каких-либо проблем), но с дополнительными методами, которые в основном используются быстрыми токенизаторами.

Помимо возможностей параллелизации, ключевая функциональность быстрых токенизаторов заключается в том, что они всегда отслеживают исходный диапазон текстов, из которых берутся окончательные токены — функция, которую мы называем *offset mapping* (*отображением смещения*). Это, в свою очередь, открывает доступ к таким функциям, как сопоставление каждого слова со сгенерированными им токенами или сопоставление каждого символа исходного текста с токеном, в котором он находится, и наоборот.

Посмотрим на пример:

```py
from transformers import AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("bert-base-cased")
example = "My name is Sylvain and I work at Hugging Face in Brooklyn."
encoding = tokenizer(example)
print(type(encoding))
```

Как упоминалось ранее, мы получаем объект `BatchEncoding` на выходе токенизатора:

```python out
<class 'transformers.tokenization_utils_base.BatchEncoding'>
```

Поскольку класс `AutoTokenizer` по умолчанию выбирает быстрый токенизатор, мы можем использовать дополнительные методы, предоставляемые объектом `BatchEncoding`. У нас есть два способа проверить, является ли наш токенизатор быстрым или медленным. Мы можем проверить атрибут `is_fast` у `tokenizer`:

```python
tokenizer.is_fast
```

```python out
True
```

или проверить тот же атрибут у объекта `encoding`:

```python
encoding.is_fast
```

```python out
True
```

Давайте посмотрим, что нам позволяет делать быстрый токенизатор. Во-первых, мы можем получить доступ к токенам без необходимости конвертировать идентификаторы обратно в токены:

```py
encoding.tokens()
```

```python out
['[CLS]', 'My', 'name', 'is', 'S', '##yl', '##va', '##in', 'and', 'I', 'work', 'at', 'Hu', '##gging', 'Face', 'in',
 'Brooklyn', '.', '[SEP]']
```

В этом случае токен в индексе 5 — это `##yl`, который является частью слова «Sylvain» в исходном предложении. Мы также можем использовать метод `word_ids()`, чтобы получить индекс слова, из которого происходит каждый токен:

```py
encoding.word_ids()
```

```python out
[None, 0, 1, 2, 3, 3, 3, 3, 4, 5, 6, 7, 8, 8, 9, 10, 11, 12, None]
```

Мы видим, что специальные токены токенизатора `[CLS]` и `[SEP]` сопоставляются с `None`, а затем каждый токен сопоставляется со словом, из которого он происходит. Это особенно полезно, чтобы определить, находится ли токен в начале слова или два токена находятся в одном слове. Мы могли бы полагаться на префикс `##` для этого, но он работает только для BERT-подобных токенизаторов; этот метод работает для любого типа токенизатора, если он быстрый. В следующей главе мы увидим, как мы можем использовать эту возможность для правильного применения меток, которые у нас есть для каждого слова, к токенам в таких задачах, как распознавание именованных сущностей (NER) и определение частей речи (POS). Мы также можем использовать его для маскировки всех токенов, исходящих от одного и того же слова, при моделировании маскированного языка (метод, называемый _whole word masking_ (_маскированием всего слова_)).

<Tip>

Понятие о том, что такое слово, сложное. Например, считается ли «I'll» (сокращение от «I will») одним или двумя словами? На самом деле это зависит от токенизатора и применяемой им операции предварительной токенизации. Некоторые токенизаторы просто разбивают на пробелы, поэтому они будут рассматривать это как одно слово. Другие используют знаки препинания поверх пробелов, поэтому будем считать это двумя словами.

✏️ **Попробуйте!** Создайте токенизатор из контрольных точек `bert-base-cased` и `roberta-base` и токенизируйте с их помощью «81s». Что вы наблюдаете? 

</Tip>

Точно так же существует метод `sentence_ids()`, который мы можем использовать для сопоставления токена с предложением, из которого он был получен (хотя в этом случае `token_type_ids`, возвращаемый токенизатором, может дать нам ту же информацию).

Наконец, мы можем преобразовать любое слово или токен в символы в исходном тексте и наоборот с помощью методов ]`word_to_chars()` или `token_to_chars()` и `char_to_word()` или `char_to_token()`. Например, метод `word_ids()` сообщил нам, что `##yl` является частью слова с индексом 3, но какое это слово в предложении? Мы можем узнать так:

```py
start, end = encoding.word_to_chars(3)
example[start:end]
```

```python out
Sylvain
```

Как мы упоминали ранее, все это основано на том факте, что быстрый токенизатор отслеживает диапазон текста, из которого поступает каждый токен, в списке *offsets* (*смещений*). Чтобы проиллюстрировать их использование, далее мы покажем, как вручную воспроизвести результаты конвейера `token-classification`.

<Tip>

✏️ **Попробуйте!** Создайте свой собственный пример текста и посмотрите, сможете ли вы понять, какие токены связаны с идентификатором слова, а также как извлечь диапазоны символов для одного слова. В качестве бонуса попробуйте использовать два предложения как входные данные и посмотрите, понятны ли вам идентификаторы предложений.

</Tip>

## `token-classification` как часть пайплайна [[inside-the-token-classification-pipeline]]

В [Главе 1](/course/ru/chapter1) мы впервые попробовали применить NER — где задача состоит в том, чтобы определить, какие части текста соответствуют таким сущностям, как люди, места или организации — с пайплайном 🤗 Transformers `pipeline()`. Затем, в [Главе 2](/course/ru/chapter2), мы увидели, как конвейер объединяет три этапа, необходимые для получения прогнозов из необработанного текста: токенизация, передача входных данных через модель и постобработка. Первые два шага в конвейере `token-classification` такие же, как и в любом другом конвейере, но постобработка немного сложнее — давайте посмотрим!

{#if fw === 'pt'}

<Youtube id="0E7ltQB7fM8"/>

{:else}

<Youtube id="PrX4CjrVnNc"/>

{/if}

### Получение первоначального результата с помощью пайплайна[[getting-the-base-results-with-the-pipeline]]

Во-первых, давайте возьмем пайплайн классификации токенов, чтобы мы могли получить некоторые результаты для сравнения. По умолчанию используется модель [`dbmdz/bert-large-cased-finetuned-conll03-english`](https://huggingface.co/dbmdz/bert-large-cased-finetuned-conll03-english); она решает задачу NER для предложений:

```py
from transformers import pipeline

token_classifier = pipeline("token-classification")
token_classifier("My name is Sylvain and I work at Hugging Face in Brooklyn.")
```

```python out
[{'entity': 'I-PER', 'score': 0.9993828, 'index': 4, 'word': 'S', 'start': 11, 'end': 12},
 {'entity': 'I-PER', 'score': 0.99815476, 'index': 5, 'word': '##yl', 'start': 12, 'end': 14},
 {'entity': 'I-PER', 'score': 0.99590725, 'index': 6, 'word': '##va', 'start': 14, 'end': 16},
 {'entity': 'I-PER', 'score': 0.9992327, 'index': 7, 'word': '##in', 'start': 16, 'end': 18},
 {'entity': 'I-ORG', 'score': 0.97389334, 'index': 12, 'word': 'Hu', 'start': 33, 'end': 35},
 {'entity': 'I-ORG', 'score': 0.976115, 'index': 13, 'word': '##gging', 'start': 35, 'end': 40},
 {'entity': 'I-ORG', 'score': 0.98879766, 'index': 14, 'word': 'Face', 'start': 41, 'end': 45},
 {'entity': 'I-LOC', 'score': 0.99321055, 'index': 16, 'word': 'Brooklyn', 'start': 49, 'end': 57}]
```

Модель правильно идентифицировала каждый токен, соответствующий «Sylvain», как человека, каждый токен, соответствующий «Hugging Face», как организацию, а токен «Brooklyn» как местоположение. Мы также можем попросить конвейер сгруппировать токены, соответствующие одному и тому же объекту:

```py
from transformers import pipeline

token_classifier = pipeline("token-classification", aggregation_strategy="simple")
token_classifier("My name is Sylvain and I work at Hugging Face in Brooklyn.")
```

```python out
[{'entity_group': 'PER', 'score': 0.9981694, 'word': 'Sylvain', 'start': 11, 'end': 18},
 {'entity_group': 'ORG', 'score': 0.97960204, 'word': 'Hugging Face', 'start': 33, 'end': 45},
 {'entity_group': 'LOC', 'score': 0.99321055, 'word': 'Brooklyn', 'start': 49, 'end': 57}]
```

Выбранная стратегия `aggregation_strategy` изменит оценки, вычисленные для каждого сгруппированного объекта. В случае `simple` оценка — это просто среднее значение оценок каждого токена в данном объекте: например, оценка «Sylvain» — это среднее значение оценок, которые мы видели в предыдущем примере для токенов «S». , `##yl`, `##va` и `##in`. Другие доступные стратегии:

- `"first"`: оценка каждой сущности — это оценка первого токена этой сущности (так, для «Sylvain» это будет 0.993828, оценка токена «S»)
- `"max"`: оценка каждой сущности — это максимальная оценка токенов в этой сущности (так, для «Hugging Face» это будет 0.98879766, оценка «Face»)
- `"average"`: где оценка каждой сущности представляет собой среднюю оценку слов, составляющих эту сущность (так, для «Sylvain» не будет никакой разницы с `simple` стратегией, но «Hugging Face» будет иметь оценку 0.9819, среднее значение баллов за «Hugging», 0.975, и «Face», 0.98879)

Теперь давайте посмотрим, как получить эти результаты без использования функции `pipeline()`!

### От входных данных к предсказаниям[[from-inputs-to-predictions]]

{#if fw === 'pt'}

Сначала нам нужно токенизировать наш ввод и передать его через модель. Это делается точно так же, как в [Главе 2](/course/ru/chapter2); мы создаем экземпляр токенизатора и модели, используя классы `AutoXxx`, а затем используем их в нашем примере:

```py
from transformers import AutoTokenizer, AutoModelForTokenClassification

model_checkpoint = "dbmdz/bert-large-cased-finetuned-conll03-english"
tokenizer = AutoTokenizer.from_pretrained(model_checkpoint)
model = AutoModelForTokenClassification.from_pretrained(model_checkpoint)

example = "My name is Sylvain and I work at Hugging Face in Brooklyn."
inputs = tokenizer(example, return_tensors="pt")
outputs = model(**inputs)
```

Поскольку здесь мы используем `AutoModelForTokenClassification`, мы получаем один набор логитов для каждого токена во входной последовательности:

```py
print(inputs["input_ids"].shape)
print(outputs.logits.shape)
```

```python out
torch.Size([1, 19])
torch.Size([1, 19, 9])
```

{:else}

Сначала нам нужно токенизировать наш ввод и передать его через модель. Это делается точно так же, как в [Главе 2](/course/ru/chapter2); мы создаем экземпляр токенизатора и модели, используя классы `TFAutoXxx`, а затем используем их в нашем примере:

```py
from transformers import AutoTokenizer, TFAutoModelForTokenClassification

model_checkpoint = "dbmdz/bert-large-cased-finetuned-conll03-english"
tokenizer = AutoTokenizer.from_pretrained(model_checkpoint)
model = TFAutoModelForTokenClassification.from_pretrained(model_checkpoint)

example = "My name is Sylvain and I work at Hugging Face in Brooklyn."
inputs = tokenizer(example, return_tensors="tf")
outputs = model(**inputs)
```

Поскольку здесь мы используем `TFAutoModelForTokenClassification` here, мы получаем один набор логитов для каждого токена во входной последовательности:

```py
print(inputs["input_ids"].shape)
print(outputs.logits.shape)
```

```python out
(1, 19)
(1, 19, 9)
```

{/if}

У нас есть батч с 1 последовательностью из 19 токенов, а модель имеет 9 различных меток, поэтому выходные данные модели имеют форму 1 x 19 x 9. Как и для конвейера классификации текста, мы используем функцию softmax для преобразования этих логитов к вероятностям, и мы берем argmax для получения прогнозов (обратите внимание, что мы можем взять argmax для логитов, потому что softmax не меняет порядок):

{#if fw === 'pt'}

```py
import torch

probabilities = torch.nn.functional.softmax(outputs.logits, dim=-1)[0].tolist()
predictions = outputs.logits.argmax(dim=-1)[0].tolist()
print(predictions)
```

{:else}

```py
import tensorflow as tf

probabilities = tf.math.softmax(outputs.logits, axis=-1)[0]
probabilities = probabilities.numpy().tolist()
predictions = tf.math.argmax(outputs.logits, axis=-1)[0]
predictions = predictions.numpy().tolist()
print(predictions)
```

{/if}

```python out
[0, 0, 0, 0, 4, 4, 4, 4, 0, 0, 0, 0, 6, 6, 6, 0, 8, 0, 0]
```

Атрибут `model.config.id2label` содержиит соответствия индексов и лейблов, которые мы можем использовать для получения осмысленных предсказаний:

```py
model.config.id2label
```

```python out
{0: 'O',
 1: 'B-MISC',
 2: 'I-MISC',
 3: 'B-PER',
 4: 'I-PER',
 5: 'B-ORG',
 6: 'I-ORG',
 7: 'B-LOC',
 8: 'I-LOC'}
```

Как мы видели ранее, имеется 9 меток: «O» — это метка для токенов, которые не находятся ни в одной именованной сущности (это означает «outside»), и у нас есть две метки для каждого типа сущности (разное, человек, организация и местонахождение). Метка «B-XXX» указывает, что токен находится в начале сущности «XXX», а метка «I-XXX» указывает, что токен находится внутри сущности «XXX». Например, в текущем примере мы ожидаем, что наша модель классифицирует токен `S` как `B-PER` (начало личности), а токены `##yl`, `##va` и `## in` как `I-PER` (внутри личности).

Вы можете подумать, что в этом случае модель была неправильной, поскольку она дала метку «I-PER» всем четырем этим токенам, но это не совсем так. На самом деле существует два формата для этих меток `B-` и `I-`: *IOB1* и *IOB2*. Формат IOB2 (розовый ниже) — это тот, который мы ввели, тогда как в формате IOB1 (синий) метки, начинающиеся с `B-`, используются только для разделения двух смежных объектов одного типа. Модель, которую мы используем, была точно настроена на наборе данных с использованием этого формата, поэтому она присваивает метку `I-PER` маркеру `S`.

<div class="flex justify-center">
<img class="block dark:hidden" src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter6/IOB_versions.svg" alt="IOB1 vs IOB2 format"/>
<img class="hidden dark:block" src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter6/IOB_versions-dark.svg" alt="IOB1 vs IOB2 format"/>
</div>

С помощью этого сопоставления мы готовы воспроизвести (почти полностью) результаты первого пайплайна — мы можем просто получить оценку и метку каждого токена, который не был классифицирован как `O`:

```py
results = []
tokens = inputs.tokens()

for idx, pred in enumerate(predictions):
    label = model.config.id2label[pred]
    if label != "O":
        results.append(
            {"entity": label, "score": probabilities[idx][pred], "word": tokens[idx]}
        )

print(results)
```

```python out
[{'entity': 'I-PER', 'score': 0.9993828, 'index': 4, 'word': 'S'},
 {'entity': 'I-PER', 'score': 0.99815476, 'index': 5, 'word': '##yl'},
 {'entity': 'I-PER', 'score': 0.99590725, 'index': 6, 'word': '##va'},
 {'entity': 'I-PER', 'score': 0.9992327, 'index': 7, 'word': '##in'},
 {'entity': 'I-ORG', 'score': 0.97389334, 'index': 12, 'word': 'Hu'},
 {'entity': 'I-ORG', 'score': 0.976115, 'index': 13, 'word': '##gging'},
 {'entity': 'I-ORG', 'score': 0.98879766, 'index': 14, 'word': 'Face'},
 {'entity': 'I-LOC', 'score': 0.99321055, 'index': 16, 'word': 'Brooklyn'}]
```

Это очень похоже на то, что у нас было раньше, за одним исключением: пайплайн также дал нам информацию о `начале` и `конце` каждой сущности в исходном предложении. Здесь в игру вступит наше отображение смещения. Чтобы получить смещения, нам просто нужно установить `return_offsets_mapping=True`, когда мы применяем токенизатор к нашим входам:

```py
inputs_with_offsets = tokenizer(example, return_offsets_mapping=True)
inputs_with_offsets["offset_mapping"]
```

```python out
[(0, 0), (0, 2), (3, 7), (8, 10), (11, 12), (12, 14), (14, 16), (16, 18), (19, 22), (23, 24), (25, 29), (30, 32),
 (33, 35), (35, 40), (41, 45), (46, 48), (49, 57), (57, 58), (0, 0)]
```

Каждый кортеж представляет собой фрагмент текста, соответствующий каждому токену, где `(0, 0)` зарезервировано для специальных токенов. Ранее мы видели, что токен с индексом 5 — это `##yl`, который здесь имеет `(12, 14)` в качестве смещения. Если мы возьмем соответствующий фрагмент в нашем примере:

```py
example[12:14]
```
мы получаем правильный диапазон текста без `##`:

```python out
yl
```

Используя это, мы теперь можем завершить дополнить предыдущие результаты:

```py
results = []
inputs_with_offsets = tokenizer(example, return_offsets_mapping=True)
tokens = inputs_with_offsets.tokens()
offsets = inputs_with_offsets["offset_mapping"]

for idx, pred in enumerate(predictions):
    label = model.config.id2label[pred]
    if label != "O":
        start, end = offsets[idx]
        results.append(
            {
                "entity": label,
                "score": probabilities[idx][pred],
                "word": tokens[idx],
                "start": start,
                "end": end,
            }
        )

print(results)
```

```python out
[{'entity': 'I-PER', 'score': 0.9993828, 'index': 4, 'word': 'S', 'start': 11, 'end': 12},
 {'entity': 'I-PER', 'score': 0.99815476, 'index': 5, 'word': '##yl', 'start': 12, 'end': 14},
 {'entity': 'I-PER', 'score': 0.99590725, 'index': 6, 'word': '##va', 'start': 14, 'end': 16},
 {'entity': 'I-PER', 'score': 0.9992327, 'index': 7, 'word': '##in', 'start': 16, 'end': 18},
 {'entity': 'I-ORG', 'score': 0.97389334, 'index': 12, 'word': 'Hu', 'start': 33, 'end': 35},
 {'entity': 'I-ORG', 'score': 0.976115, 'index': 13, 'word': '##gging', 'start': 35, 'end': 40},
 {'entity': 'I-ORG', 'score': 0.98879766, 'index': 14, 'word': 'Face', 'start': 41, 'end': 45},
 {'entity': 'I-LOC', 'score': 0.99321055, 'index': 16, 'word': 'Brooklyn', 'start': 49, 'end': 57}]
```

То же самое, что мы получили от первого пайплайна!

### Группирование сущностей[[grouping-entities]]

Использование смещений для определения начального и конечного ключей для каждого объекта удобно, но эта информация не является строго необходимой. Однако когда мы хотим сгруппировать объекты вместе, смещения избавят нас от большого количества беспорядочного кода. Например, если мы хотим сгруппировать токены `Hu`, `##gging` и `Face`, мы можем создать специальные правила, которые говорят, что первые два должны быть присоединены при удалении `##`, а `Face` должно быть добавлено через пробел, так как оно не начинается с `##`, но это будет работать только для этого конкретного типа токенизатора. Нам пришлось бы написать еще один набор правил для токенизатора SentencePiece или Byte-Pair-Encoding (обсуждается далее в этой главе).

Со смещениями весь этот пользовательский код исчезает: мы просто можем взять диапазон в исходном тексте, который начинается с первого токена и заканчивается последним токеном. Итак, в случае токенов `Hu`, `##gging` и `Face` мы должны начинать с 33-го символа (начало `Hu`) и заканчивать перед 45-м символом (конец `Face`):

```py
example[33:45]
```

```python out
Hugging Face
```

Чтобы написать код, который выполняет пост-обработку прогнозов при группировке объектов, мы сгруппируем объекты, которые следуют друг за другом и помечены `I-XXX`, за исключением первого, который может быть помечен как `B-XXX` или `I-XXX` (таким образом, мы прекращаем группировать сущность, когда получаем `O`, новый тип сущности или `B-XXX`, которые говорят нам, что начинается сущность того же типа):

```py
import numpy as np

results = []
inputs_with_offsets = tokenizer(example, return_offsets_mapping=True)
tokens = inputs_with_offsets.tokens()
offsets = inputs_with_offsets["offset_mapping"]

idx = 0
while idx < len(predictions):
    pred = predictions[idx]
    label = model.config.id2label[pred]
    if label != "O":
        # Удаляем  B- или I-
        label = label[2:]
        start, _ = offsets[idx]

        # Забираем все токены с I-
        all_scores = []
        while (
            idx < len(predictions)
            and model.config.id2label[predictions[idx]] == f"I-{label}"
        ):
            all_scores.append(probabilities[idx][pred])
            _, end = offsets[idx]
            idx += 1

        # Score - средний score для всех токенов в сгруппированной сущности
        score = np.mean(all_scores).item()
        word = example[start:end]
        results.append(
            {
                "entity_group": label,
                "score": score,
                "word": word,
                "start": start,
                "end": end,
            }
        )
    idx += 1

print(results)
```

И мы получили такой же результат, как и во втором пайплайне!

```python out
[{'entity_group': 'PER', 'score': 0.9981694, 'word': 'Sylvain', 'start': 11, 'end': 18},
 {'entity_group': 'ORG', 'score': 0.97960204, 'word': 'Hugging Face', 'start': 33, 'end': 45},
 {'entity_group': 'LOC', 'score': 0.99321055, 'word': 'Brooklyn', 'start': 49, 'end': 57}]
```

Другой пример задачи, где эти смещения чрезвычайно полезны, — это ответы на вопросы. Погружение в этот конвейер, которое мы сделаем в следующем разделе, также позволит нам взглянуть на одну последнюю функцию токенизаторов в библиотеке 🤗 Transformers: обработка токенов переполнения, когда мы усекаем ввод до заданной длины.
